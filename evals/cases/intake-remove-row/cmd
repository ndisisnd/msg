# E4 — --remove-row deletes exactly one row and never renumbers: the gap at #2
# stays, and every other line of the ledger is byte-identical (file golden).
"$REPO/.claude/scripts/script-intake-stamp.sh" INTAKE.md 2 --remove-row
