#!/bin/sh
if [ "$1" = "devices" ]; then
  echo "List of devices attached"
  printf "emulator-5554\tdevice\n"
  exit 0
fi
if [ "$1" = "-s" ] && [ "$3" = "emu" ] && [ "$4" = "avd" ]; then
  echo Pixel_4_API_30
  exit 0
fi
exit 0
