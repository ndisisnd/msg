# A5 — throwaway two-commit repo; no remote, no network.
export GIT_AUTHOR_DATE="2026-01-15T00:00:00Z" GIT_COMMITTER_DATE="2026-01-15T00:00:00Z"
G="git -c user.email=fixture@example.invalid -c user.name=fixture -c commit.gpgsign=false"
$G init -q -b main .
mkdir -p src; echo a > src/a.txt; $G add -A; $G commit -qm c1
echo b > src/a.txt; $G add -A; $G commit -qm c2
# A5 FIRES — the base ref cannot be diffed at all ⇒ degrade to tier L (AC-TS10),
# never to the smallest selection.
"$REPO/.claude/scripts/script-tier-resolve.sh" origin/nope --affected-ratio 0.01 --fan-in-pct 0.1
