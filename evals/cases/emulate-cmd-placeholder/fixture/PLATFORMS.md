# PLATFORMS

An untouched template row, a dashed row and a blank cell are all the same
statement: "not configured". None of them is a command.

| platform | rollback_possible | release_model | required_buckets | emulate_cmd |
|---|---|---|---|---|
| android | yes | submission | e2e | [USER: optional local run command] |
