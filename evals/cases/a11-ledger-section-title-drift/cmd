# A11 — an H2 containing "findings" exists under a drifted title; appending a
# second findings section would leave consumers reading the wrong one.
printf '%s' '[{"severity":"Major","what":"§5 error handling omits the offline case","fix":"add an offline branch","why":"pre-merge PRD-consistency gate"}]' | python3 "$REPO/.claude/scripts/script-ledger.py" prd.md --auditor P --date 2026-01-15
