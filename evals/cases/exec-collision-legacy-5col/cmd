# v5.4 P4 read-tolerance — the same rows in the legacy 5-column shape produce a
# byte-identical decomposition, so an existing PRD schedules exactly as before.
python3 "$REPO/.claude/scripts/script-em-exec-collision.py" exec.md --waves 2>/dev/null
