# A14 — the §3 id column is headed `F-ID`, so every row digests with an empty
# `id` and no consumer can address a feature by F-ID. Refuse before emitting.
python3 "$REPO/.claude/scripts/script-prd-digest.py" prd.md --slice build --feature F1
