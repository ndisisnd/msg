# A22 — a short exec row (its last cell is simply missing) used to be dropped
# with no trace, making the ticket it points at look unpointed.
python3 "$REPO/.claude/scripts/script-eng-plan-shape.py" plan.md --checks 1,2,3,4,5,6
