# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
**/.DS_Store

# ── Dependencies ───────────────────────────────────────────────────────────────
node_modules/

# ── Build output ───────────────────────────────────────────────────────────────
dist/

# ── Logs & caches ─────────────────────────────────────────────────────────────
*.log
.cache/
al-*.jsonl
**/ux-style-cache.md
.claude/msg/cache/

# ── Runtime artefacts ─────────────────────────────────────────────────────────
**/run/
**/schemas.json
.claude/scheduled_tasks.lock
.claude/.headroom_wrap_marker.json
.pre-merge/

# ── Local config ──────────────────────────────────────────────────────────────
settings.local.json

# ── Working dirs (plans, features) ────────────────────────────────────────────
# Root evals/ is the committed regression-eval suite (v5.1) — deliberately NOT
# ignored; the legacy scratch suite lives under update/, which is.
plans/
features/
roadmap/
devkit/
eval-msg-workflow*
handoff/
update/
update-plan.md
update-plan-done.md
update-plan-gui.md
update-plan-gui-done.md
.claude/skills/improve/
.claude/skills/**/*-plan.md
# …but component protocol files are source, not scratch plans (e.g. manual-test-plan, C22)
!.claude/skills/**/protocol-*-plan.md
# …and eng's fix-plan ref is source too (renamed from report-fix.md in v5)
!.claude/skills/eng/refs/plan/fix-plan.md
.claude/kermit/state.json

# Headroom
.tokensave
__pycache__/
.serena/

# Machine-local agent notes (auto-generated by `headroom learn`)
# Root-anchored: msg's own root AGENTS.md is machine-local scratch, but Codex
# reads AGENTS.md as project memory, so nested ones (eval fixtures, scaffolded
# sample projects) are source and must stay committable.
/AGENTS.md
CLAUDE.local.md
