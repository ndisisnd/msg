# remove-manifest.txt — retired install artifacts scrubbed on every update.
#
# One entry per line, exact paths relative to ~/.claude/. `#` starts a comment
# (whole-line or inline). install.sh reads this from the cloned repo after the
# install-skills loop and `rm -rf`s each entry under ~/.claude/ (absent = silent
# skip, so it is idempotent). Guardrails in install.sh reject anything that is
# not an exact `skills/<name>` or `scripts/<file>` path — no globs, no `..`, no
# absolute paths — so a retirement can never reach outside those two dirs.

# skills retired from the global install
skills/msg-init      # folded into /msg --init (was hardcoded in install.sh)
skills/docu
skills/handoff
skills/ship
skills/plan
skills/design
skills/improve       # repo-internal plan tracker, never should have shipped

# scripts orphaned by retired skills
scripts/ship-db-touch.sh
scripts/ship-find-prd.sh
